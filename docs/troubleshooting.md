# 常见问题

## 解析失败（parse error）

- 检查文件扩展名是否被语言映射覆盖。
- 对 Rust 等非默认语言，确认已安装对应 `@ast-grep/lang-*` 包并已注册动态语言。

## 无匹配

- 先用最小 pattern 验证（例如 `console.log($A)`）。
- 使用 `explain` 在目标位置检查规则是否在该语言/文件上生效。

## diff 过大

- 优先改写最小节点，避免整文件 replace。
- 将规则从“fix”降级为“只报告”，对高风险改动采用人工审阅。
